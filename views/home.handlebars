<div class="flex-row posts">
    <article class="card" style="width: 60rem">
        {{each}}
        <div class="">
            <h3>
                {{title}}
            </h3>
            <p>{{body}}</p>
        </div>

        <div class="card-text">

        </div>
        {{/each}}
    </article>
</div>

